export default {
      LIBRARY(state) {
        return state.library
      },
      BUSKET(state) {
        return state.busket
      },
      CREATED_BOOK(state) {
        return state.createdBook
      },
      SEARCH_VALUE(state) {
        return state.searchValue
      }

}